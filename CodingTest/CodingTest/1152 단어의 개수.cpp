#include <iostream>
#include <string>
#include <algorithm>

using namespace std;

string s;
char c;
int ret, temp;
int main()
{/*
	getline(cin, s);
	for (int i = 0; i < s.size(); ++i)
	{
		if (s[i] == ' ')
			++temp;
	}
	cout << s.size() - temp << "\n";*/
	while (cin >> s)
	{
		++ret;
	}
	cout << ret;
	return 0;
}